<div class="row">
    <div class="col">
        <p>Wyników <%- products.length %> na <%- resultsCount %></p>
        <nav>
           <ul class="pagination">
            <% for (let i =1; i <= pagesCount; i++) { %>
               <li class="page-item <%- page == i ? 'active' : '' %>">
                <a class="page-link" data-page="<%- i %>" href="#">
                    <%- i %>
                </a>
            </li>
            <% } %>
           </ul>
        </nav>
    </div>
</div>

